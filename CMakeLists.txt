################################################################################
#  THIS FILE IS 100% GENERATED BY ZPROJECT; DO NOT EDIT EXCEPT EXPERIMENTALLY  #
#  Read the zproject/README.md for information about making permanent changes. #
################################################################################

########################################################################
# Project setup
########################################################################
cmake_minimum_required(VERSION 2.8.8)
project(czguide)
enable_language(C)
enable_testing()

set(SOURCE_DIR "${CMAKE_CURRENT_SOURCE_DIR}")
set(CMAKE_EXPORT_COMPILE_COMMANDS 1)
# Select flags
SET(CMAKE_C_FLAGS_RELEASE "-O3")

# Will be used to add flags to pkg-config useful when apps want to statically link
set(pkg_config_libs_private "")

########################################################################
# options
########################################################################
OPTION (ENABLE_DRAFTS "Build and install draft classes and methods" ON)
IF (ENABLE_DRAFTS)
    ADD_DEFINITIONS (-DCZGUIDE_BUILD_DRAFT_API)
ENDIF (ENABLE_DRAFTS)

########################################################################
# platform.h
########################################################################
include(CheckIncludeFile)
CHECK_INCLUDE_FILE("linux/wireless.h" HAVE_LINUX_WIRELESS_H)
CHECK_INCLUDE_FILE("net/if_media.h" HAVE_NET_IF_MEDIA_H)

include(CheckFunctionExists)
CHECK_FUNCTION_EXISTS("getifaddrs" HAVE_GETIFADDRS)
CHECK_FUNCTION_EXISTS("freeifaddrs" HAVE_FREEIFADDRS)

include(CheckIncludeFiles)
check_include_files("sys/socket.h;net/if.h" HAVE_NET_IF_H)
if (NOT HAVE_NET_IF_H)
    CHECK_INCLUDE_FILE("net/if.h" HAVE_NET_IF_H)
endif()

file(WRITE "${SOURCE_DIR}/src/platform.h.in" "
#cmakedefine HAVE_LINUX_WIRELESS_H
#cmakedefine HAVE_NET_IF_H
#cmakedefine HAVE_NET_IF_MEDIA_H
#cmakedefine HAVE_GETIFADDRS
#cmakedefine HAVE_FREEIFADDRS
")

configure_file("${SOURCE_DIR}/src/platform.h.in" "${SOURCE_DIR}/src/platform.h")

#The MSVC C compiler is too out of date,
#so the sources have to be compiled as c++
if (MSVC)
    enable_language(CXX)
    file(GLOB sources "${SOURCE_DIR}/src/*.c")
    set_source_files_properties(
        ${sources}
        PROPERTIES LANGUAGE CXX
    )
    set(MORE_LIBRARIES ws2_32 Rpcrt4 Iphlpapi)
endif()

# specific case of windows UWP
if( "${CMAKE_SYSTEM_NAME}" STREQUAL "WindowsStore" AND "${CMAKE_SYSTEM_VERSION}" STREQUAL "10.0")
  ADD_DEFINITIONS(-DZMQ_HAVE_WINDOWS_UWP)
  ADD_DEFINITIONS(-D_WIN32_WINNT=_WIN32_WINNT_WIN10)
endif()


# required libraries for mingw
if (MINGW)
    set(MORE_LIBRARIES -lws2_32 -lrpcrt4 -liphlpapi)
endif()

# required libraries for cygwin
if (CYGWIN)
    set(MORE_LIBRARIES)
endif()

list(APPEND CMAKE_MODULE_PATH "${SOURCE_DIR}")
set(OPTIONAL_LIBRARIES)

########################################################################
# LIBZMQ dependency
########################################################################
find_package(libzmq REQUIRED)
IF (LIBZMQ_FOUND)
    include_directories(${LIBZMQ_INCLUDE_DIRS})
    list(APPEND MORE_LIBRARIES ${LIBZMQ_LIBRARIES})
    set(pkg_config_libs_private "${pkg_config_libs_private} -lzmq")
ELSE (LIBZMQ_FOUND)
    message( FATAL_ERROR "libzmq not found." )
ENDIF (LIBZMQ_FOUND)

########################################################################
# CZMQ dependency
########################################################################
find_package(czmq REQUIRED)
IF (CZMQ_FOUND)
    include_directories(${CZMQ_INCLUDE_DIRS})
    list(APPEND MORE_LIBRARIES ${CZMQ_LIBRARIES})
    set(pkg_config_libs_private "${pkg_config_libs_private} -lczmq")
ELSE (CZMQ_FOUND)
    message( FATAL_ERROR "czmq not found." )
ENDIF (CZMQ_FOUND)

########################################################################
# version
########################################################################
set(CZGUIDE_VERSION_MAJOR 0)
set(CZGUIDE_VERSION_MINOR 0)
set(CZGUIDE_VERSION_PATCH 0)
set(CZGUIDE_VERSION "${CZGUIDE_VERSION_MAJOR}.${CZGUIDE_VERSION_MINOR}.${CZGUIDE_VERSION_PATCH}")
message(STATUS "Detected CZGUIDE Version - ${CZGUIDE_VERSION}")


########################################################################
# includes
########################################################################
set (czguide_headers
    include/czguide_library.h
    include/czguide.h
)

IF (ENABLE_DRAFTS)
    list(APPEND czguide_headers
        include/common.h
    )
ENDIF (ENABLE_DRAFTS)

source_group ("Header Files" FILES ${czguide_headers})
install(FILES ${czguide_headers} DESTINATION include)

########################################################################
# library
########################################################################


include_directories("${SOURCE_DIR}/src" "${SOURCE_DIR}/include")
set (czguide_sources
)

IF (ENABLE_DRAFTS)
    list (APPEND czguide_sources
        src/common.c
    )
ENDIF (ENABLE_DRAFTS)

IF (ENABLE_DRAFTS)
    list (APPEND czguide_sources
        src/czguide_private_selftest.c
    )
ENDIF (ENABLE_DRAFTS)

source_group("Source Files" FILES ${czguide_sources})


option(CZGUIDE_BUILD_SHARED "Whether or not to build the shared object" ON)
option(CZGUIDE_BUILD_STATIC "Whether or not to build the static archive" ON)

if (NOT CZGUIDE_BUILD_SHARED AND NOT CZGUIDE_BUILD_STATIC)
  message(FATAL_ERROR "Neither static nor shared library build enabled")
endif()

IF (NOT MSVC)
  # avoid building everything twice for shared + static
  # only on *nix, as Windows needs different preprocessor defines in static builds
  add_library (czguide_objects OBJECT ${czguide_sources})
  set_property(TARGET czguide_objects PROPERTY POSITION_INDEPENDENT_CODE ON)
ENDIF (NOT MSVC)

# shared
if (CZGUIDE_BUILD_SHARED)
  IF (MSVC)
    add_library(czguide SHARED ${czguide_sources})
  ELSE (MSVC)
    add_library(czguide SHARED $<TARGET_OBJECTS:czguide_objects>)
  ENDIF (MSVC)

  set_target_properties (czguide PROPERTIES
    PUBLIC_HEADER "${public_headers}"
    DEFINE_SYMBOL "CZGUIDE_EXPORTS"
    SOVERSION "0"
    VERSION "${CZGUIDE_VERSION}"
    COMPILE_DEFINITIONS "DLL_EXPORT"
    OUTPUT_NAME "czguide"
    PREFIX "lib"
  )

  target_link_libraries(czguide
    ${ZEROMQ_LIBRARIES} ${MORE_LIBRARIES}
  )

  install(TARGETS czguide
    LIBRARY DESTINATION "lib${LIB_SUFFIX}" # .so file
    ARCHIVE DESTINATION "lib${LIB_SUFFIX}" # .lib file
    RUNTIME DESTINATION bin                # .dll file
  )

  target_include_directories(czguide
    PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/include
  )
endif()

# static
if (CZGUIDE_BUILD_STATIC)
  IF (MSVC)
    add_library(czguide-static STATIC ${czguide_sources})
  ELSE (MSVC)
    add_library(czguide-static STATIC $<TARGET_OBJECTS:objects>)
  ENDIF (MSVC)

  set_target_properties(czguide-static PROPERTIES
    PUBLIC_HEADER "${public_headers}"
    COMPILE_DEFINITIONS "CZGUIDE_STATIC"
    OUTPUT_NAME "czguide"
    PREFIX "lib"
  )

  target_link_libraries(czguide-static
    ${ZEROMQ_LIBRARIES} ${MORE_LIBRARIES}
  )

  install(TARGETS czguide-static
    LIBRARY DESTINATION "lib${LIB_SUFFIX}" # .so file
    ARCHIVE DESTINATION "lib${LIB_SUFFIX}" # .lib file
    RUNTIME DESTINATION bin                # .dll file
  )

  target_include_directories(czguide-static
    PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/include
  )
  target_compile_definitions(czguide-static
    PUBLIC CZGUIDE_STATIC
  )

endif()



########################################################################
# pkgconfig
########################################################################
set (VERSION "0.0.0")
set (prefix "${CMAKE_INSTALL_PREFIX}")
set (exec_prefix "\${prefix}")
set (libdir "\${prefix}/lib${LIB_SUFFIX}")
set (includedir "\${prefix}/include")
IF (ENABLE_DRAFTS)
    set (pkg_config_defines "-DCZGUIDE_BUILD_DRAFT_API=1")
ELSE (ENABLE_DRAFTS)
    set (pkg_config_defines "")
ENDIF (ENABLE_DRAFTS)
configure_file(
    "${SOURCE_DIR}/src/libczguide.pc.in"
    "${SOURCE_DIR}/src/libczguide.pc"
@ONLY)

install(
    FILES "${SOURCE_DIR}/src/libczguide.pc"
    DESTINATION "lib${LIB_SUFFIX}/pkgconfig"
)

########################################################################
# executables
########################################################################
add_executable(
    lpclient
    "${SOURCE_DIR}/src/lpclient.c"
)
if (TARGET czguide)
target_link_libraries(
    lpclient
    czguide
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
if (NOT TARGET czguide AND TARGET czguide-static)
target_link_libraries(
    lpclient
    czguide-static
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
install(TARGETS lpclient
    RUNTIME DESTINATION bin
)
add_executable(
    lpserver
    "${SOURCE_DIR}/src/lpserver.c"
)
if (TARGET czguide)
target_link_libraries(
    lpserver
    czguide
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
if (NOT TARGET czguide AND TARGET czguide-static)
target_link_libraries(
    lpserver
    czguide-static
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
install(TARGETS lpserver
    RUNTIME DESTINATION bin
)
add_executable(
    printerrno
    "${SOURCE_DIR}/src/printerrno.c"
)
if (TARGET czguide)
target_link_libraries(
    printerrno
    czguide
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
if (NOT TARGET czguide AND TARGET czguide-static)
target_link_libraries(
    printerrno
    czguide-static
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
install(TARGETS printerrno
    RUNTIME DESTINATION bin
)
add_executable(
    hwserver
    "${SOURCE_DIR}/src/hwserver.c"
)
if (TARGET czguide)
target_link_libraries(
    hwserver
    czguide
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
if (NOT TARGET czguide AND TARGET czguide-static)
target_link_libraries(
    hwserver
    czguide-static
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
install(TARGETS hwserver
    RUNTIME DESTINATION bin
)
add_executable(
    hwclient
    "${SOURCE_DIR}/src/hwclient.c"
)
if (TARGET czguide)
target_link_libraries(
    hwclient
    czguide
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
if (NOT TARGET czguide AND TARGET czguide-static)
target_link_libraries(
    hwclient
    czguide-static
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
install(TARGETS hwclient
    RUNTIME DESTINATION bin
)
add_executable(
    version
    "${SOURCE_DIR}/src/version.c"
)
if (TARGET czguide)
target_link_libraries(
    version
    czguide
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
if (NOT TARGET czguide AND TARGET czguide-static)
target_link_libraries(
    version
    czguide-static
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
install(TARGETS version
    RUNTIME DESTINATION bin
)
add_executable(
    wuserver
    "${SOURCE_DIR}/src/wuserver.c"
)
if (TARGET czguide)
target_link_libraries(
    wuserver
    czguide
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
if (NOT TARGET czguide AND TARGET czguide-static)
target_link_libraries(
    wuserver
    czguide-static
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
install(TARGETS wuserver
    RUNTIME DESTINATION bin
)
add_executable(
    wuclient
    "${SOURCE_DIR}/src/wuclient.c"
)
if (TARGET czguide)
target_link_libraries(
    wuclient
    czguide
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
if (NOT TARGET czguide AND TARGET czguide-static)
target_link_libraries(
    wuclient
    czguide-static
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
install(TARGETS wuclient
    RUNTIME DESTINATION bin
)
add_executable(
    tasksink
    "${SOURCE_DIR}/src/tasksink.c"
)
if (TARGET czguide)
target_link_libraries(
    tasksink
    czguide
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
if (NOT TARGET czguide AND TARGET czguide-static)
target_link_libraries(
    tasksink
    czguide-static
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
install(TARGETS tasksink
    RUNTIME DESTINATION bin
)
add_executable(
    taskvent
    "${SOURCE_DIR}/src/taskvent.c"
)
if (TARGET czguide)
target_link_libraries(
    taskvent
    czguide
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
if (NOT TARGET czguide AND TARGET czguide-static)
target_link_libraries(
    taskvent
    czguide-static
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
install(TARGETS taskvent
    RUNTIME DESTINATION bin
)
add_executable(
    taskwork
    "${SOURCE_DIR}/src/taskwork.c"
)
if (TARGET czguide)
target_link_libraries(
    taskwork
    czguide
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
if (NOT TARGET czguide AND TARGET czguide-static)
target_link_libraries(
    taskwork
    czguide-static
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
install(TARGETS taskwork
    RUNTIME DESTINATION bin
)
add_executable(
    msreader
    "${SOURCE_DIR}/src/msreader.c"
)
if (TARGET czguide)
target_link_libraries(
    msreader
    czguide
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
if (NOT TARGET czguide AND TARGET czguide-static)
target_link_libraries(
    msreader
    czguide-static
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
install(TARGETS msreader
    RUNTIME DESTINATION bin
)
add_executable(
    mspoller
    "${SOURCE_DIR}/src/mspoller.c"
)
if (TARGET czguide)
target_link_libraries(
    mspoller
    czguide
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
if (NOT TARGET czguide AND TARGET czguide-static)
target_link_libraries(
    mspoller
    czguide-static
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
install(TARGETS mspoller
    RUNTIME DESTINATION bin
)
add_executable(
    rrclient
    "${SOURCE_DIR}/src/rrclient.c"
)
if (TARGET czguide)
target_link_libraries(
    rrclient
    czguide
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
if (NOT TARGET czguide AND TARGET czguide-static)
target_link_libraries(
    rrclient
    czguide-static
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
install(TARGETS rrclient
    RUNTIME DESTINATION bin
)
add_executable(
    rrworker
    "${SOURCE_DIR}/src/rrworker.c"
)
if (TARGET czguide)
target_link_libraries(
    rrworker
    czguide
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
if (NOT TARGET czguide AND TARGET czguide-static)
target_link_libraries(
    rrworker
    czguide-static
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
install(TARGETS rrworker
    RUNTIME DESTINATION bin
)
add_executable(
    rrbroker
    "${SOURCE_DIR}/src/rrbroker.c"
)
if (TARGET czguide)
target_link_libraries(
    rrbroker
    czguide
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
if (NOT TARGET czguide AND TARGET czguide-static)
target_link_libraries(
    rrbroker
    czguide-static
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
install(TARGETS rrbroker
    RUNTIME DESTINATION bin
)
add_executable(
    msgqueue
    "${SOURCE_DIR}/src/msgqueue.c"
)
if (TARGET czguide)
target_link_libraries(
    msgqueue
    czguide
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
if (NOT TARGET czguide AND TARGET czguide-static)
target_link_libraries(
    msgqueue
    czguide-static
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
install(TARGETS msgqueue
    RUNTIME DESTINATION bin
)
add_executable(
    wuproxy
    "${SOURCE_DIR}/src/wuproxy.c"
)
if (TARGET czguide)
target_link_libraries(
    wuproxy
    czguide
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
if (NOT TARGET czguide AND TARGET czguide-static)
target_link_libraries(
    wuproxy
    czguide-static
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
install(TARGETS wuproxy
    RUNTIME DESTINATION bin
)
add_executable(
    czguide_selftest
    "${SOURCE_DIR}/src/czguide_selftest.c"
)
if (TARGET czguide)
target_link_libraries(
    czguide_selftest
    czguide
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()
if (NOT TARGET czguide AND TARGET czguide-static)
target_link_libraries(
    czguide_selftest
    czguide-static
    ${LIBZMQ_LIBRARIES}
    ${CZMQ_LIBRARIES}
    ${OPTIONAL_LIBRARIES}
)
endif()

########################################################################
# tests
########################################################################
set(CLASSTEST_TIMEOUT 60 CACHE STRING "Timeout of the selftest of a class")
set(TOTAL_TIMEOUT 600 CACHE STRING "Timout of the total testsuite")

if(UNIX)
    find_program(MEMORYCHECK_COMMAND valgrind)
    set(MEMORYCHECK_COMMAND_OPTIONS "--leak-check=full --show-reachable=yes
                                     --error-exitcode=1
                                     --suppressions=src/.valgrind.supp")
endif()

set(TEST_CLASSES
)

IF (ENABLE_DRAFTS)
    list (APPEND TEST_CLASSES
    common
    )
ENDIF (ENABLE_DRAFTS)

IF (ENABLE_DRAFTS)
    list (APPEND TEST_CLASSES
    private_classes
    )
ENDIF (ENABLE_DRAFTS)


add_custom_target(
    copy-selftest-ro ALL
    COMMAND ${CMAKE_COMMAND} -E copy_directory ${CMAKE_SOURCE_DIR}/src/selftest-ro ${CMAKE_BINARY_DIR}/src/selftest-ro
)

add_custom_target(
    make-selftest-rw ALL
    COMMAND ${CMAKE_COMMAND} -E make_directory ${CMAKE_BINARY_DIR}/src/selftest-rw
)

set_directory_properties(
    PROPERTIES
    ADDITIONAL_MAKE_CLEAN_FILES "${CMAKE_BINARY_DIR}/src/selftest-ro;${CMAKE_BINARY_DIR}/src/selftest-rw"
)

foreach(TEST_CLASS ${TEST_CLASSES})
    add_test(
        NAME ${TEST_CLASS}
        COMMAND czguide_selftest --continue --verbose --test ${TEST_CLASS}
    )
    set_tests_properties(
        ${TEST_CLASS}
        PROPERTIES TIMEOUT ${CLASSTEST_TIMEOUT}
    )
    set_tests_properties(
        ${TEST_CLASS}
        PROPERTIES DEPENDS "copy-selftest-ro;make-selftest-rw"
    )
endforeach(TEST_CLASS)

include(CTest)

########################################################################
# cleanup
########################################################################
add_custom_target (distclean @echo Cleaning for source distribution)

set(cmake_generated ${CMAKE_BINARY_DIR}/CMakeCache.txt
                    ${CMAKE_BINARY_DIR}/cmake_install.cmake
                    ${CMAKE_BINARY_DIR}/Makefile
                    ${CMAKE_BINARY_DIR}/CMakeFiles
                    ${CMAKE_BINARY_DIR}/CTestTestfile.cmake
                    ${CMAKE_BINARY_DIR}/DartConfiguration.tcl
                    ${CMAKE_BINARY_DIR}/Testing
                    ${CMAKE_BINARY_DIR}/compile_commands.json
                    ${CMAKE_BINARY_DIR}/src/platform.h
                    ${CMAKE_BINARY_DIR}/src/libczguide.pc
                    ${CMAKE_BINARY_DIR}/src/libczguide.so
                    ${CMAKE_BINARY_DIR}/src/czguide_selftest
                    ${CMAKE_BINARY_DIR}/src/lpclient
                    ${CMAKE_BINARY_DIR}/src/lpserver
                    ${CMAKE_BINARY_DIR}/src/printerrno
                    ${CMAKE_BINARY_DIR}/src/hwserver
                    ${CMAKE_BINARY_DIR}/src/hwclient
                    ${CMAKE_BINARY_DIR}/src/version
                    ${CMAKE_BINARY_DIR}/src/wuserver
                    ${CMAKE_BINARY_DIR}/src/wuclient
                    ${CMAKE_BINARY_DIR}/src/tasksink
                    ${CMAKE_BINARY_DIR}/src/taskvent
                    ${CMAKE_BINARY_DIR}/src/taskwork
                    ${CMAKE_BINARY_DIR}/src/msreader
                    ${CMAKE_BINARY_DIR}/src/mspoller
                    ${CMAKE_BINARY_DIR}/src/rrclient
                    ${CMAKE_BINARY_DIR}/src/rrworker
                    ${CMAKE_BINARY_DIR}/src/rrbroker
                    ${CMAKE_BINARY_DIR}/src/msgqueue
                    ${CMAKE_BINARY_DIR}/src/wuproxy
                    ${CMAKE_BINARY_DIR}/src/czguide_selftest
)

add_custom_command(
    DEPENDS clean
    COMMENT "distribution clean"
    COMMAND rm
    ARGS    -rf CMakeTmp ${cmake_generated}
    TARGET  distclean
)

########################################################################
# summary
########################################################################
message ("")
message (STATUS "******************* Configuration Summary *******************")
message (STATUS "General:")
message (STATUS "  Version           :   ${VERSION}")
message (STATUS "  System            :   ${CMAKE_SYSTEM_NAME}")
message (STATUS "  C compiler        :   ${CMAKE_C_COMPILER}")
message (STATUS "  Debug C flags     :   ${CMAKE_C_FLAGS_DEBUG} ${CMAKE_C_FLAGS}")
message (STATUS "  Release C flags   :   ${CMAKE_C_FLAGS_RELEASE} ${CMAKE_C_FLAGS}")
message (STATUS "  Build type        :   ${CMAKE_BUILD_TYPE}")
message (STATUS "  Static build      :   ${CZGUIDE_BUILD_STATIC}")
message (STATUS "  Shared build      :   ${CZGUIDE_BUILD_SHARED}")
IF (ENABLE_DRAFTS)
message (STATUS "  Draft API         :   Yes")
ELSE (ENABLE_DRAFTS)
message (STATUS "  Draft API         :   No")
ENDIF (ENABLE_DRAFTS)
message (STATUS "")
message (STATUS "Dependencies:")
include(FeatureSummary)
feature_summary (WHAT ALL FATAL_ON_MISSING_REQUIRED_PACKAGES)
message (STATUS "")
message (STATUS "Install:")
message (STATUS "  Install prefix    :" "${CMAKE_INSTALL_PREFIX}")
message (STATUS "")
message (STATUS "************************* Options ***************************")
message (STATUS "Options:")
message (STATUS "  Use the Draft API (default = yes):")
message (STATUS "  -DENABLE-DRAFTS=[yes|no]")
message (STATUS "")
message (STATUS "*************************************************************")
message (STATUS "Configuration complete! Now procced with:")
message (STATUS "  'make'                compile the project")
message (STATUS "  'make test'           run the project's selftest")
message (STATUS "  'make install'        install the project to ${CMAKE_INSTALL_PREFIX}")
message (STATUS "")
message (STATUS "Further options are:")
message (STATUS "  'ctest -T memcheck'   run the project's selftest with")
message (STATUS "                        valgrind to check for memory leaks")
message (STATUS "")

################################################################################
#  THIS FILE IS 100% GENERATED BY ZPROJECT; DO NOT EDIT EXCEPT EXPERIMENTALLY  #
#  Read the zproject/README.md for information about making permanent changes. #
################################################################################
